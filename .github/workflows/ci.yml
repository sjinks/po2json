name: CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      id-token: write
      attestations: write
    env:
      CXX: clang++-17
    steps:
      - name: Check out code
        uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332 # v4.1.7

      - name: Install dependencies
        uses: ./.github/actions/install-dependencies

      - name: Build
        run: cmake -B build -DCMAKE_BUILD_TYPE=Debug && cmake --build build

      - name: Test
        run: ctest --test-dir build --output-on-failure

      - name: Attest
        uses: actions/attest-build-provenance@897ed5eab6ed058a474202017ada7f40bfa52940 # v1.0.0
        id: attest
        with:
          subject-path: '${{ github.workspace }}/build/src/po2json'

      - name: Print Attestation
        run: |
          echo "${{ steps.attest.outputs.bundle-path }}"
          cat "${{ steps.attest.outputs.bundle-path }}"
          jq . "${{ steps.attest.outputs.bundle-path }}"
